# Nexus 任务图谱

## 当前完成状态

- [x] 设计并实现 v3 clean architecture。
- [x] 用 Registry + Broker + signed Agent 替代旧 token 模式。
- [x] 统一到一个 `install.sh`。
- [x] Linux / OpenWrt / Windows / VSC Agent 纳管。
- [x] Oracle global registry + EU broker 运行。
- [x] ThinkCenter CN broker 运行。
- [x] `oracle`、`thinkcenter`、`n1`、`victus`、`victus-wsl`、`vsc` 已 approved。
- [x] 6 个节点均已通过 Nexus command job 验收。
- [x] 6 个 Nexus SSH public keys 已发布并同步到主要节点。
- [x] 当前 dashboard 已恢复旧布局并更新 v3 状态。
- [x] 当前安装脚本已推送 GitHub `release/core-2aea394`。

## 当前待明确/可选目标

- [ ] `ax3600` 是否作为独立 OpenWrt Agent 自领取，还是长期由 ThinkCenter managed target 指挥。
- [ ] `elitebook` 是否正式纳管为 EU device。
- [ ] VSC inbound SSH：需要 KU Leuven/VSC 官方 SSH certificate 或门户 key 注册；普通 Nexus `authorized_keys` 不足以绕过 HPC 登录策略。
- [ ] Dashboard 是否接入安全的只读 `/status.json` 实时状态。
- [ ] 是否把 SSH mesh 验收矩阵做成 dashboard 只读视图。

## 不再作为主线推进的目标

这些目标只保留思想，不再保持旧代码运行：

- Supabase CAS 热队列；
- 旧 FastMCP SSE 服务；
- 旧 Browser Bridge / Agent Council；
- Herdr / SuperAssistant 实验；
- 多个安装脚本并存；
- 每设备 API token。

详见根目录 `其他目标.md`。
